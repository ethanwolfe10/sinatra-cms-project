<h1> Welcome <%= @current_doctor.username.capitalize! %>! </h1>
<br>

<h2 style="border-top: 0px;">Here Are All of Your Appointments: </h2>
    <% if @appts.empty? %>
        <p style="text-align: center;">No Appointments! Click <a href="/appointments/new">Here</a> to Create Appointment</p>       
        <p style="border-bottom: 1px solid wheat;"></p>
    <% else %>
        <% @appts.each do |appt| %>
            <p>-<%= appt.dog.name %> the <%= appt.dog.breed.name %> (<%= appt.date %> at <%= appt.time %> @ <%= appt.dog.shelter.name %>)</p>
            <br>
            <% end %>
            <p style='text-align: center;'>(Click <a href="/appointments/<%= @current_doctor.slug %>/edit">Here</a> To Edit Appointments) Or (Click <a href="/appointments/new">Here</a> to Create Appointments)</p>
        <p style="border-bottom: 1px solid wheat;"></p>
    <% end %>


<h2 style="border-top: 0px;">Here Are All of Your Active Shelters: </h2>
<br>
<% if @shelters.empty? %>
    <p style='text-align: center;'>You Have No Active Shelters! Click <a href="/doctors/<%= @current_doctor.slug %>/newshelter"> Here </a> to Add Your First!
<% else %>
    <p style='text-align: center;'>(Click <a href="/dogs/new"> Here </a> to Add a New Dog)</p>
    <% @shelters.each do |shelter| %>
        <div class="dog_div">
        <h2 class="shelter" style="border-top: 0px;"><%= shelter.name %>: (Website - <%= shelter.website %>)</h2>
            <div class="dog">
                <h4 style='text-align: left; color: sandybrown;'>Dogs: </h4>
            
                <% shelter.dogs.each do |dog| %>
                    <% if shelter.id == dog.shelter_id %>
                        <p><a href='/dogs/<%= dog.slug %>'><%= dog.name %> the <%=dog.breed.name %></a></p>
                    <% end %>
                <% end %>
                <br>
            </div>
        </div>
    <% end %>
<% end %>
   
<br>


 